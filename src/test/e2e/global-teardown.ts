/**
 * Global teardown for E2E tests
 *
 * Exported from global-setup.ts for Playwright config compatibility.
 */

import { globalTeardown as teardown } from './global-setup';

export default teardown;
